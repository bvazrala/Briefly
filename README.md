# Briefly

Briefly is an ESP32 device that reads you the morning without handing you a phone. The alarm runs on the hardware itself, so it still goes off when the network doesn't, and the news and weather it reads you are summarized by a model running locally rather than in someone else's cloud. 
